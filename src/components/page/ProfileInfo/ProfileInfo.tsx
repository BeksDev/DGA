import { BaseMainProfileInfo } from '@components/base';

const ProfileInfo = ({ data }: any) => {
  return (
    <div>
      <BaseMainProfileInfo data={data} />
    </div>
  );
};

export default ProfileInfo;
